[project]
name = "taskflow"
version = "0.3.0"
description = "A tiny task store — the reference project CoreSentinel governs end to end"
requires-python = ">=3.9"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.4"]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = ["tests"]
