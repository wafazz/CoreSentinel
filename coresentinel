#!/usr/bin/env bash
# CoreSentinel POSIX Executable Wrapper
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
python3 "$SCRIPT_DIR/coresentinel.py" "$@" || python "$SCRIPT_DIR/coresentinel.py" "$@"
